
<p><%= question_header(@question) %></p>

<%= form_for([:admin, @test, @question]) do |form|%>
  <p>
    <%= form.label :body %>
    <%= form.text_field :body %>
  </p>
  <br>
  <p>
    <%= form.submit %>
  </p> 
<% end %>